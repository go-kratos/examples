data:
  database:
    driver: "postgres"
    source: "host=localhost port=5432 user=postgres password=@Abcd123456 dbname=kratos_monolithic sslmode=disable"
    migrate: true
    debug: false
    max_idle_connections: 25
    max_open_connections: 25
    connection_max_lifetime: 300s

  redis:
    addr: "localhost:6379"
    password: "123456"
    dial_timeout: 10s
    read_timeout: 0.4s
    write_timeout: 0.6s